{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Cross Renamer Tool","text":""},{"location":"#description","title":"Description","text":"<p>The Cross Renamer Tool is a production-ready renaming utility for Autodesk Maya and Blender. It covers the full range of renaming operations an artist needs on a daily basis.</p> <p>Warning</p> <p>For now, the tool only works in Autodesk Maya</p> <p>Cross Renamer Tool</p> <p></p> <p>Installation User Manual Developer Docs API</p>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#requires","title":"Requires","text":"<ul> <li>Autodesk Maya 2026+</li> <li>Blender 5.0+</li> <li>Python 3.10+</li> <li>PySide6 (bundled with Maya 2025+; install separately for Maya 2024)</li> </ul>"},{"location":"installation/#launch","title":"Launch","text":""},{"location":"installation/#option-1-scripts-folder","title":"Option 1 - Scripts folder","text":"<ol> <li> <p>Clone or download the repository into your Maya scripts folder: <pre><code># Windows\nC:/Users/&lt;you&gt;/Documents/maya/scripts/\n\n# macOS / Linux\n~/maya/scripts/\n</code></pre></p> </li> <li> <p>Open the Scripts Editor in Maya, create a Python tab and paste: <pre><code>from crossrenamertool import launcher\nlancher.run()\n</code></pre></p> </li> </ol> <p>Tip</p> <p>Create a shelf button for launch the tool quickly. If you want to save the tool in a shelf, follow [How To Guide] Create Shelf Button</p>"},{"location":"installation/#options-2-usersetuppy","title":"Options 2 - <code>userSetup.py</code>","text":"<p>Add the following code to your <code>userSetup.py</code> file (located in your Maya scripts folder): <pre><code># userSetup.py\nimport maya.utils as utils\n\ndef launch_cross_renamer():\n    from crossrenamertool import launcher\n    launcher.run()\n\nutils.executeDeferred(launch_cross_renamer)\n</code></pre></p> <p>Note</p> <p><code>executeDeferred</code>  ensures Maya has fully loaded its UI before the tool launches</p>"},{"location":"installation/#options-3-installpy-script","title":"Options 3 - <code>install.py</code> script","text":"<ol> <li>Open the Scripts Editor in Maya, create a Python tab.</li> <li>Paste the following code and update the <code>paste</code> variable with the path to the <code>src</code> folder on your machine: <pre><code># install.py \u2014 to execute into the script editor\nfrom maya import cmds\n\n# Button code\nbutton_code = \"\"\"\nimport sys\n\npath = r\"path/to/module/src\"  # update this\nif path not in sys.path:\n    sys.path.append(path)\n\nfrom crossrenamertool.launcher import run\nrun()\n\"\"\"\n\n# Icon\nicon = \"menuIconModify.png\"\n\ncmds.shelfButton(\n    parent=\"Custom\",\n    label=\"Renamer\",\n    annotation=\"Open Cross Renamer Tool\",\n    sourceType=\"python\",\n    command=button_code,\n    image1=icon,\n    style=\"iconAndTextVertical\",\n)\n\nprint(\"Shelf button installed !\")\n</code></pre></li> <li> <p>Execute the script by pressing Ctrl+Enter or clicking the Execute button.</p> </li> <li> <p>A Renamer button will appear in your Custom shelf.</p> </li> </ol> <p>Tip</p> <p>The <code>Custom</code> shelf must already exist in Maya. If it doesn't, create it first via Shelf Editor \u2192 New Shelf.</p>"},{"location":"API/","title":"API","text":"<ul> <li>Controller</li> <li>Model<ul> <li>Blender API</li> <li>Maya API</li> </ul> </li> <li>View Overview<ul> <li>Blender</li> <li>Maya<ul> <li>MainView</li> <li>Widgets<ul> <li>About Dialog</li> <li>Case Widget</li> <li>Insert Remove Widget</li> <li>Prefix Suffix Widget</li> <li>Preset Combobox</li> <li>Preset Dialog</li> <li>Rename Widget</li> <li>Search Replace Widget</li> <li>Selection Mode Widget</li> <li>Utils Widget</li> </ul> </li> </ul> </li> </ul> </li> <li>Core Overview<ul> <li>Constants</li> <li>Presets</li> <li>Renamer</li> </ul> </li> </ul>"},{"location":"API/controller/","title":"Controller","text":""},{"location":"API/controller/#crossrenamertool.main-classes","title":"Classes","text":""},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController","title":"<code>CrossRenamerToolController()</code>","text":"<p>Controller for the module.</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def __init__(self):\n    self._view = None\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController-functions","title":"Methods:","text":""},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.add_characters","title":"<code>add_characters(mode, text, position, from_start)</code>","text":"<p>Add characters to a text at a specific position.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>text</code> <code>str</code> <p>characters to add</p> required <code>position</code> <code>int</code> <p>position to insert</p> required <code>from_start</code> <code>bool</code> <p>True if insert from start else False.</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def add_characters(self, mode, text, position, from_start) -&gt; dict[str, str]:\n    \"\"\"Add characters to a text at a specific position.\n\n    Args:\n        mode (str): mode of selection\n        text (str): characters to add\n        position (int): position to insert\n        from_start (bool): True if insert from start else False.\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return maya_api.add_characters(mode, text, position, from_start)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.add_prefix","title":"<code>add_prefix(mode, prefix='')</code>","text":"<p>Add prefix to nodes.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>prefix</code> <code>str</code> <p>prefix. Defaults to \"\".</p> <code>''</code> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def add_prefix(self, mode, prefix=\"\") -&gt; dict[str, str]:\n    \"\"\"Add prefix to nodes.\n\n    Args:\n        mode (str): mode of selection\n        prefix (str, optional): prefix. Defaults to \"\".\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    return maya_api.add_prefix(mode=mode, prefix=prefix)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.add_suffix","title":"<code>add_suffix(mode, suffix='')</code>","text":"<p>Add suffix to nodes.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>suffix</code> <code>str</code> <p>suffix to add. Defaults to \"\".</p> <code>''</code> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def add_suffix(self, mode, suffix=\"\") -&gt; dict[str, str]:\n    \"\"\"Add suffix to nodes.\n\n    Args:\n        mode (str): mode of selection\n        suffix (str, optional): suffix to add. Defaults to \"\".\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    return maya_api.add_suffix(mode, suffix)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.auto_fix_duplicates","title":"<code>auto_fix_duplicates()</code>","text":"<p>Auto rename duplicates nodes.</p> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def auto_fix_duplicates(self) -&gt; dict[str, str]:\n    \"\"\"Auto rename duplicates nodes.\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    return maya_api.auto_fix_duplicates()\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.delete_workspace_control","title":"<code>delete_workspace_control(workspace_name)</code>","text":"<p>Close and delete an existing Maya workspace control.</p> <p>Parameters:</p> Name Type Description Default <code>workspace_name</code> <code>str</code> <p>name of the workspace control to delete</p> required Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def delete_workspace_control(self, workspace_name: str) -&gt; None:\n    \"\"\"Close and delete an existing Maya workspace control.\n\n    Args:\n        workspace_name (str): name of the workspace control to delete\n\n    \"\"\"\n    maya_api.delete_workspace_control(workspace_name)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.fix_shape_name","title":"<code>fix_shape_name()</code>","text":"<p>Rename shapes to match their transform name.</p> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: {old_name: new_name}</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def fix_shape_name(self) -&gt; dict[str, str]:\n    \"\"\"Rename shapes to match their transform name.\n\n    Returns:\n        dict[str, str]: {old_name: new_name}\n\n    \"\"\"\n    return maya_api.fix_shape_name()\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.get_duplicates","title":"<code>get_duplicates()</code>","text":"<p>Get duplicates nodes in the scene.</p> <p>Returns:</p> Type Description <p>list[str]: list of duplicates nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def get_duplicates(self):\n    \"\"\"Get duplicates nodes in the scene.\n\n    Returns:\n        list[str]: list of duplicates nodes\n\n    \"\"\"\n    return maya_api.get_duplicates()\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.launch","title":"<code>launch()</code>","text":"<p>Launch the application by displaying the view.</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def launch(self) -&gt; None:\n    \"\"\"Launch the application by displaying the view.\"\"\"\n    if self._view is None:\n        log.error(\"No view attached to controller\")\n        raise RuntimeError(\"Cannot launch: no view attached\")\n\n    self._view.launch_app()\n    log.info(\"Cross Renamer Tool launched successfully\")\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.remove_characters","title":"<code>remove_characters(mode, position, count, from_start)</code>","text":"<p>Delete characters at given position in the node name.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>position</code> <code>int</code> <p>start index of deletion</p> required <code>count</code> <code>int</code> <p>number of characters to delete</p> required <code>from_start</code> <code>bool</code> <p>True count from start else from end.</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def remove_characters(self, mode, position, count, from_start) -&gt; dict[str, str]:\n    \"\"\"Delete characters at given position in the node name.\n\n    Args:\n        mode (str): mode of selection\n        position (int): start index of deletion\n        count (int): number of characters to delete\n        from_start (bool): True count from start else from end.\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return maya_api.remove_characters(mode, position, count, from_start)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.remove_prefix","title":"<code>remove_prefix(mode, prefix='')</code>","text":"<p>Remove prefix to nodes.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>prefix</code> <code>str</code> <p>prefix. Defaults to \"\".</p> <code>''</code> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def remove_prefix(self, mode, prefix=\"\") -&gt; dict[str, str]:\n    \"\"\"Remove prefix to nodes.\n\n    Args:\n        mode (str): mode of selection\n        prefix (str, optional): prefix. Defaults to \"\".\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    return maya_api.remove_prefix(mode=mode, prefix=prefix)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.remove_suffix","title":"<code>remove_suffix(mode, suffix='')</code>","text":"<p>Remove suffix to nodes.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>suffix</code> <code>str</code> <p>suffix. Defaults to \"\".</p> <code>''</code> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def remove_suffix(self, mode, suffix=\"\") -&gt; dict[str, str]:\n    \"\"\"Remove suffix to nodes.\n\n    Args:\n        mode (str): mode of selection\n        suffix (str, optional): suffix. Defaults to \"\".\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    return maya_api.remove_suffix(mode=mode, suffix=suffix)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.rename_nodes","title":"<code>rename_nodes(mode, base_name='', padding=constants.DEFAULT_PADDING, start=constants.DEFAULT_START, step=constants.DEFAULT_STEP)</code>","text":"<p>Rename selected nodes.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>base_name</code> <code>str</code> <p>base name. Defaults to \"\".</p> <code>''</code> <code>padding</code> <code>int</code> <p>number of 0. Defaults to 3.</p> <code>DEFAULT_PADDING</code> <code>start</code> <code>int</code> <p>start number. Defaults to 1.</p> <code>DEFAULT_START</code> <code>step</code> <code>int</code> <p>increment between each number. Defaults to 1.</p> <code>DEFAULT_STEP</code> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def rename_nodes(\n    self,\n    mode,\n    base_name=\"\",\n    padding=constants.DEFAULT_PADDING,\n    start=constants.DEFAULT_START,\n    step=constants.DEFAULT_STEP,\n) -&gt; dict[str, str]:\n    \"\"\"Rename selected nodes.\n\n    Args:\n        mode (str): mode of selection\n        base_name (str, optional): base name. Defaults to \"\".\n        padding (int, optional): number of 0. Defaults to 3.\n        start (int, optional): start number. Defaults to 1.\n        step (int, optional): increment between each number. Defaults to 1.\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    return maya_api.rename_nodes(mode, base_name=base_name, padding=padding, start=start, step=step)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.search_replace","title":"<code>search_replace(search_name, replace_name, case)</code>","text":"<p>Search and replace name in node.</p> <p>Parameters:</p> Name Type Description Default <code>search_name</code> <code>str</code> <p>name to find</p> required <code>replace_name</code> <code>str</code> <p>new name to replace</p> required <code>case</code> <code>bool</code> <p>case sensitive</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def search_replace(self, search_name, replace_name, case) -&gt; dict[str, str]:\n    \"\"\"Search and replace name in node.\n\n    Args:\n        search_name (str): name to find\n        replace_name (str): new name to replace\n        case (bool): case sensitive\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    return maya_api.search_replace(search_name, replace_name, case)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.select_item","title":"<code>select_item(index, datas)</code>","text":"<p>Select the node by the duplicates table.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>index in the table</p> required <code>datas</code> <code>dict[str, str]</code> <p>duplicates node dictionary</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: name of the selected node</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def select_item(self, index, datas) -&gt; str | None:\n    \"\"\"Select the node by the duplicates table.\n\n    Args:\n        index (int): index in the table\n        datas (dict[str, str]): duplicates node dictionary\n\n    Returns:\n        str | None: name of the selected node\n\n    \"\"\"\n    return maya_api.select_item(index, datas)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.set_view","title":"<code>set_view(view)</code>","text":"<p>Attach a view instance to the controller.</p> <p>Parameters:</p> Name Type Description Default <code>view</code> <code>CrossRenamerToolView</code> <p>view instance that will be managed by the controller.</p> required Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def set_view(self, view) -&gt; None:\n    \"\"\"Attach a view instance to the controller.\n\n    Args:\n        view (CrossRenamerToolView): view instance that will be managed by the controller.\n\n    \"\"\"\n    global _WINDOW\n    self._view = view\n    _WINDOW = self._view\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.swap_side","title":"<code>swap_side(mode)</code>","text":"<p>Swap side indicator in the node.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>selected mode</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: new name swap</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def swap_side(self, mode) -&gt; dict[str, str]:\n    \"\"\"Swap side indicator in the node.\n\n    Args:\n        mode (str): selected mode\n\n    Returns:\n        dict[str, str]: new name swap\n\n    \"\"\"\n    return maya_api.swap_side(mode=mode)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.text_to_camel","title":"<code>text_to_camel(mode)</code>","text":"<p>Convert text to camel.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def text_to_camel(self, mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to camel.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return maya_api.text_to_camel(mode)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.text_to_capitalize","title":"<code>text_to_capitalize(mode)</code>","text":"<p>Convert text to capitalize.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def text_to_capitalize(self, mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to capitalize.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return maya_api.text_to_capitalize(mode)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.text_to_lowercase","title":"<code>text_to_lowercase(mode)</code>","text":"<p>Convert text to lowercase.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def text_to_lowercase(self, mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to lowercase.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return maya_api.text_to_lowercase(mode)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.text_to_pascal","title":"<code>text_to_pascal(mode)</code>","text":"<p>Convert text to pascal.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def text_to_pascal(self, mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to pascal.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return maya_api.text_to_pascal(mode)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.text_to_snake","title":"<code>text_to_snake(mode)</code>","text":"<p>Convert text to snake.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def text_to_snake(self, mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to snake.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return maya_api.text_to_snake(mode)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.text_to_title","title":"<code>text_to_title(mode)</code>","text":"<p>Convert text to title.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def text_to_title(self, mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to title.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return maya_api.text_to_title(mode)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main.CrossRenamerToolController.text_to_uppercase","title":"<code>text_to_uppercase(mode)</code>","text":"<p>Convert text to uppercase.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def text_to_uppercase(self, mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to uppercase.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return maya_api.text_to_uppercase(mode)\n</code></pre>"},{"location":"API/controller/#crossrenamertool.main-functions","title":"Functions:","text":""},{"location":"API/controller/#crossrenamertool.main.create_controller","title":"<code>create_controller()</code>","text":"<p>Create Cross Renamer Tool.</p> <p>Returns:</p> Type Description <p>return the controller.</p> Source code in <code>src/crossrenamertool/main.py</code> <pre><code>def create_controller():\n    \"\"\"Create Cross Renamer Tool.\n\n    Returns:\n        return the controller.\n\n    \"\"\"\n    controller = CrossRenamerToolController()\n    controller.set_view(view=app_view.create_view(controller=controller))\n\n    controller.launch()\n    return controller\n</code></pre>"},{"location":"API/core/","title":"Core","text":"<ul> <li>Constants</li> <li>Presets</li> <li>Renamer</li> </ul>"},{"location":"API/core/constants/","title":"Constants","text":""},{"location":"API/core/presets/","title":"Presets","text":"<p>Presets manager for prefixes and suffixes.</p>"},{"location":"API/core/presets/#crossrenamertool.core.presets-functions","title":"Functions:","text":""},{"location":"API/core/presets/#crossrenamertool.core.presets.add_preset","title":"<code>add_preset(preset_type, value)</code>","text":"<p>Add a value to the presets of a given type.</p> <p>Parameters:</p> Name Type Description Default <code>preset_type</code> <code>str</code> <p>\"prefixes\" or \"suffixes\"</p> required <code>value</code> <code>str</code> <p>value to add</p> required <p>Returns:</p> Type Description <code>list</code> <p>list[str]: updated list of presets</p> Source code in <code>src/crossrenamertool/core/presets.py</code> <pre><code>def add_preset(preset_type: str, value: str) -&gt; list:\n    \"\"\"Add a value to the presets of a given type.\n\n    Args:\n        preset_type (str): \"prefixes\" or \"suffixes\"\n        value (str): value to add\n\n    Returns:\n        list[str]: updated list of presets\n\n    \"\"\"\n    presets = load_presets(preset_type)\n\n    if value in presets:\n        log.warning(f\"'{value}' already exists in {preset_type}.\")\n        return presets\n\n    presets.append(value)\n    save_presets(preset_type, presets)\n    return presets\n</code></pre>"},{"location":"API/core/presets/#crossrenamertool.core.presets.load_presets","title":"<code>load_presets(preset_type)</code>","text":"<p>Load presets.</p> <p>Parameters:</p> Name Type Description Default <code>preset_type</code> <code>str</code> <p>\"prefixes\" or \"suffixes\"</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: list of presets</p> Source code in <code>src/crossrenamertool/core/presets.py</code> <pre><code>def load_presets(preset_type: str) -&gt; list[str]:\n    \"\"\"Load presets.\n\n    Args:\n        preset_type (str): \"prefixes\" or \"suffixes\"\n\n    Returns:\n        list[str]: list of presets\n\n    \"\"\"\n    return _load(_get_path(preset_type))\n</code></pre>"},{"location":"API/core/presets/#crossrenamertool.core.presets.remove_preset","title":"<code>remove_preset(preset_type, value)</code>","text":"<p>Remove a value from the presets of a given type.</p> <p>Parameters:</p> Name Type Description Default <code>preset_type</code> <code>str</code> <p>\"prefixes\" or \"suffixes\"</p> required <code>value</code> <code>str</code> <p>value to remove</p> required <p>Returns:</p> Type Description <code>list</code> <p>list[str]: updated list of presets</p> Source code in <code>src/crossrenamertool/core/presets.py</code> <pre><code>def remove_preset(preset_type: str, value: str) -&gt; list:\n    \"\"\"Remove a value from the presets of a given type.\n\n    Args:\n        preset_type (str): \"prefixes\" or \"suffixes\"\n        value (str): value to remove\n\n    Returns:\n        list[str]: updated list of presets\n\n    \"\"\"\n    presets = load_presets(preset_type)\n\n    if value not in presets:\n        log.warning(f\"'{value}' not found in {preset_type}.\")\n        return presets\n\n    presets.remove(value)\n    save_presets(preset_type, presets)\n    return presets\n</code></pre>"},{"location":"API/core/presets/#crossrenamertool.core.presets.save_presets","title":"<code>save_presets(preset_type, presets)</code>","text":"<p>Save presets for a given type.</p> <p>Parameters:</p> Name Type Description Default <code>preset_type (str) </code> <p>\"prefixes\" or \"suffixes\"</p> required <code>presets</code> <code>list[str]</code> <p>list of presets to save</p> required Source code in <code>src/crossrenamertool/core/presets.py</code> <pre><code>def save_presets(preset_type: str, presets: list) -&gt; None:\n    \"\"\"Save presets for a given type.\n\n    Args:\n        preset_type (str) : \"prefixes\" or \"suffixes\"\n        presets (list[str]): list of presets to save\n\n    \"\"\"\n    _save(_get_path(preset_type), presets)\n</code></pre>"},{"location":"API/core/renamer/","title":"Renamer","text":""},{"location":"API/core/renamer/#crossrenamertool.core.renamer-functions","title":"Functions:","text":""},{"location":"API/core/renamer/#crossrenamertool.core.renamer.add_characters","title":"<code>add_characters(node, text, position, from_start)</code>","text":"<p>Insert text at a given position in the node name.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>node name</p> required <code>text</code> <code>str</code> <p>text to insert</p> required <code>position</code> <code>int</code> <p>index position</p> required <code>from_start</code> <code>bool</code> <p>True, count from start. False, from end</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>new node name</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def add_characters(node, text, position, from_start) -&gt; str:\n    \"\"\"Insert text at a given position in the node name.\n\n    Args:\n        node (str): node name\n        text (str): text to insert\n        position (int): index position\n        from_start (bool): True, count from start. False, from end\n\n    Returns:\n        str: new node name\n\n    \"\"\"\n    index = position if from_start else -position or len(node)\n    return node[:index] + text + node[index:]\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.add_prefix","title":"<code>add_prefix(base_name, prefix)</code>","text":"<p>Add prefix to the node.</p> <p>Parameters:</p> Name Type Description Default <code>base_name</code> <code>str</code> <p>base name</p> required <code>prefix</code> <code>str</code> <p>prefix</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>renamed name</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def add_prefix(base_name, prefix) -&gt; str:\n    \"\"\"Add prefix to the node.\n\n    Args:\n        base_name (str): base name\n        prefix (str): prefix\n\n    Returns:\n        str: renamed name\n\n    \"\"\"\n    if not base_name:\n        log.warning(\"You don't have any base name on the node.\")\n        return None\n    if not prefix:\n        log.warning(\"Can't add prefix to your name.\")\n        return None\n\n    return f\"{prefix}_{base_name}\"\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.add_suffix","title":"<code>add_suffix(base_name, suffix)</code>","text":"<p>Add suffix to the node.</p> <p>Parameters:</p> Name Type Description Default <code>base_name</code> <code>str</code> <p>base name</p> required <code>suffix</code> <code>str</code> <p>suffix to add</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str | None</code> <p>renamed name</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def add_suffix(base_name, suffix) -&gt; str | None:\n    \"\"\"Add suffix to the node.\n\n    Args:\n        base_name (str): base name\n        suffix (str): suffix to add\n\n    Returns:\n        str: renamed name\n\n    \"\"\"\n    if not base_name:\n        log.warning(\"You don't have any base name on the node.\")\n        return None\n\n    if not suffix:\n        log.warning(\"Can't add suffix to your name.\")\n        return None\n\n    return f\"{base_name}_{suffix}\"\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.fix_shape_name","title":"<code>fix_shape_name(transform_name, shape_index)</code>","text":"<p>Generate the correct shape name from its transform name.</p> <p>Parameters:</p> Name Type Description Default <code>transform_name (str) </code> <p>name of the transform node</p> required <code>shape_index (int) </code> <p>index for multiple shapes (0 = no number)</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>correct shape name</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def fix_shape_name(transform_name: str, shape_index: int) -&gt; str:\n    \"\"\"Generate the correct shape name from its transform name.\n\n    Args:\n        transform_name (str) : name of the transform node\n        shape_index (int) : index for multiple shapes (0 = no number)\n\n    Returns:\n        str: correct shape name\n\n    \"\"\"\n    if shape_index == 0:\n        return f\"{transform_name}Shape\"\n\n    return f\"{transform_name}Shape_{shape_index:03d}\"\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.remove_characters","title":"<code>remove_characters(node, position, count, from_start)</code>","text":"<p>Delete characters at given position in the node name.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>node name</p> required <code>position</code> <code>int</code> <p>start index of deletion</p> required <code>count</code> <code>int</code> <p>number of characters to delete</p> required <code>from_start</code> <code>bool</code> <p>True count from start else from end.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>new node name</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def remove_characters(node, position, count, from_start) -&gt; str:\n    \"\"\"Delete characters at given position in the node name.\n\n    Args:\n        node (str): node name\n        position (int): start index of deletion\n        count (int): number of characters to delete\n        from_start (bool): True count from start else from end.\n\n    Returns:\n        str: new node name\n\n    \"\"\"\n    if count &gt;= len(node):\n        log.warning(\"Can't delete all characters.\")\n        return node\n    index = position if from_start else len(node) - position - count\n    index = max(0, min(index, len(node)))\n    count = max(0, min(count, len(node) - index))\n\n    return node[:index] + node[index + count :]\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.remove_prefix","title":"<code>remove_prefix(node, prefix)</code>","text":"<p>Remove prefix in the name.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>node name</p> required <code>prefix</code> <code>str</code> <p>prefix to remove</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>new name</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def remove_prefix(node, prefix) -&gt; str:\n    \"\"\"Remove prefix in the name.\n\n    Args:\n        node (str): node name\n        prefix (str): prefix to remove\n\n    Returns:\n        str: new name\n\n    \"\"\"\n    if node.startswith(prefix):\n        return node[len(prefix) :]\n\n    return node\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.remove_suffix","title":"<code>remove_suffix(node, suffix)</code>","text":"<p>Remove suffix in the name.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>node name</p> required <code>suffix</code> <code>str</code> <p>suffix to remove</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>new name</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def remove_suffix(node, suffix) -&gt; str:\n    \"\"\"Remove suffix in the name.\n\n    Args:\n        node (str): node name\n        suffix (str): suffix to remove\n\n    Returns:\n        str: new name\n\n    \"\"\"\n    if node.endswith(suffix):\n        return node[: -len(suffix)]\n\n    return node\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.renaming","title":"<code>renaming(base_name, num, padding)</code>","text":"<p>Create the new name for the node.</p> <p>Parameters:</p> Name Type Description Default <code>base_name</code> <code>str</code> <p>base name of the node.</p> required <code>num</code> <code>int</code> <p>number for the padding.</p> required <code>padding</code> <code>int</code> <p>number of 0 for padding.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>new name for the node.</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def renaming(base_name, num, padding) -&gt; str:\n    \"\"\"Create the new name for the node.\n\n    Args:\n        base_name (str): base name of the node.\n        num (int): number for the padding.\n        padding (int): number of 0 for padding.\n\n    Returns:\n        str: new name for the node.\n\n    \"\"\"\n    if not base_name:\n        log.warning(\"base_name is empty.\")\n        return None\n    if num is None or padding is None:\n        return f\"{base_name}\"\n\n    return f\"{base_name}_{num:0{padding}d}\"\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.search_replace","title":"<code>search_replace(node, search_name, replace_name, case)</code>","text":"<p>Search and replace name in node.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>node selected</p> required <code>search_name</code> <code>str</code> <p>name to find</p> required <code>replace_name</code> <code>str</code> <p>new name to replace</p> required <code>case</code> <code>bool</code> <p>case sensitive</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>new name renamed</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def search_replace(node, search_name, replace_name, case) -&gt; str:\n    \"\"\"Search and replace name in node.\n\n    Args:\n        node (str): node selected\n        search_name (str): name to find\n        replace_name (str): new name to replace\n        case (bool): case sensitive\n\n    Returns:\n        str: new name renamed\n\n    \"\"\"\n    flags = 0 if case else re.IGNORECASE\n\n    if not search_name:\n        return node\n\n    return re.sub(search_name, replace_name, node, flags=flags)\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.swap_side","title":"<code>swap_side(node, swap_sides)</code>","text":"<p>Swap side indicator in the node.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>selected node</p> required <code>swap_sides</code> <code>dict[str, str]</code> <p>dictionary of the sides</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str | None</code> <p>new name with swap side</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def swap_side(node, swap_sides) -&gt; str | None:\n    \"\"\"Swap side indicator in the node.\n\n    Args:\n        node (str): selected node\n        swap_sides (dict[str, str]): dictionary of the sides\n\n    Returns:\n        str: new name with swap side\n\n    \"\"\"\n    if not swap_sides:\n        log.error(\"The swap_sides dictionary wasn't found\")\n        return None\n\n    all_sides = \"|\".join(re.escape(side) for side in swap_sides)\n    pattern = rf\"(?&lt;![a-zA-Z])({all_sides})(?![a-zA-Z])\"\n    match = re.search(pattern, node)\n\n    if not match:\n        log.warning(f\"No side indicator found in '{node}'\")\n        return node\n\n    side = match.group(1)\n    opposite = swap_sides.get(side)\n    if not opposite:\n        log.warning(f\"No opposite found for '{side}'\")\n        return node\n\n    return re.sub(pattern, opposite, node, count=1)\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.text_to_camel","title":"<code>text_to_camel(node)</code>","text":"<p>Change node name to camel.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>node</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>renamed node</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def text_to_camel(node) -&gt; str:\n    \"\"\"Change node name to camel.\n\n    Args:\n        node (str): node\n\n    Returns:\n        str: renamed node\n\n    \"\"\"\n    parts = re.split(r\"[_\\-\\s]+\", node)\n\n    return parts[0].lower() + \"\".join(part.capitalize() for part in parts[1:])\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.text_to_capitalize","title":"<code>text_to_capitalize(node)</code>","text":"<p>Change node name to capitalize.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>node</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>renamed node</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def text_to_capitalize(node) -&gt; str:\n    \"\"\"Change node name to capitalize.\n\n    Args:\n        node (str): node\n\n    Returns:\n        str: renamed node\n\n    \"\"\"\n    return node.capitalize()\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.text_to_lowercase","title":"<code>text_to_lowercase(node)</code>","text":"<p>Change node name to lowercase.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>node</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>renamed node</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def text_to_lowercase(node) -&gt; str:\n    \"\"\"Change node name to lowercase.\n\n    Args:\n        node (str): node\n\n    Returns:\n        str: renamed node\n\n    \"\"\"\n    return node.lower()\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.text_to_pascal","title":"<code>text_to_pascal(node)</code>","text":"<p>Change node name to pascal.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>node</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>renamed node</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def text_to_pascal(node) -&gt; str:\n    \"\"\"Change node name to pascal.\n\n    Args:\n        node (str): node\n\n    Returns:\n        str: renamed node\n\n    \"\"\"\n    parts = re.split(r\"[_\\-\\s]+\", node)\n\n    return \"\".join(part.capitalize() for part in parts)\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.text_to_snake","title":"<code>text_to_snake(node)</code>","text":"<p>Change node name to snake.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>node</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>renamed node</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def text_to_snake(node) -&gt; str:\n    \"\"\"Change node name to snake.\n\n    Args:\n        node (str): node\n\n    Returns:\n        str: renamed node\n\n    \"\"\"\n    node = re.sub(r\"([A-Z]+)([A-Z][a-z])\", r\"\\1_\\2\", node)\n    node = re.sub(r\"([a-z\\d])([A-Z])\", r\"\\1_\\2\", node)\n\n    return node.lower()\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.text_to_title","title":"<code>text_to_title(node)</code>","text":"<p>Change node name to title.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>node</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>renamed node</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def text_to_title(node) -&gt; str:\n    \"\"\"Change node name to title.\n\n    Args:\n        node (str): node\n\n    Returns:\n        str: renamed node\n\n    \"\"\"\n    return node.title()\n</code></pre>"},{"location":"API/core/renamer/#crossrenamertool.core.renamer.text_to_uppercase","title":"<code>text_to_uppercase(node)</code>","text":"<p>Change node name to uppercase.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>str</code> <p>node</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>renamed node</p> Source code in <code>src/crossrenamertool/core/renamer.py</code> <pre><code>def text_to_uppercase(node) -&gt; str:\n    \"\"\"Change node name to uppercase.\n\n    Args:\n        node (str): node\n\n    Returns:\n        str: renamed node\n\n    \"\"\"\n    return node.upper()\n</code></pre>"},{"location":"API/model/","title":"Model","text":"<ul> <li>Blender</li> <li>Maya</li> </ul>"},{"location":"API/model/blender_api/","title":"Blender API","text":""},{"location":"API/model/maya_api/","title":"Maya API","text":"<p>Maya adapter.</p>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api-functions","title":"Functions:","text":""},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.add_characters","title":"<code>add_characters(mode, text, position, from_start)</code>","text":"<p>Add characters to a text at a specific position.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>text</code> <code>str</code> <p>characters to add</p> required <code>position</code> <code>int</code> <p>position to insert</p> required <code>from_start</code> <code>bool</code> <p>True if insert from start else False.</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def add_characters(mode, text, position, from_start) -&gt; dict[str, str]:\n    \"\"\"Add characters to a text at a specific position.\n\n    Args:\n        mode (str): mode of selection\n        text (str): characters to add\n        position (int): position to insert\n        from_start (bool): True if insert from start else False.\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.add_characters(node, text, position, from_start))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.add_prefix","title":"<code>add_prefix(mode, prefix)</code>","text":"<p>Add prefix to nodes.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>prefix</code> <code>str</code> <p>prefix</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def add_prefix(mode, prefix) -&gt; dict[str, str]:\n    \"\"\"Add prefix to nodes.\n\n    Args:\n        mode (str): mode of selection\n        prefix (str): prefix\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.add_prefix(node, prefix))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.add_suffix","title":"<code>add_suffix(mode, suffix)</code>","text":"<p>Add suffix to nodes.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>suffix</code> <code>str</code> <p>suffix to add</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def add_suffix(mode, suffix) -&gt; dict[str, str]:\n    \"\"\"Add suffix to nodes.\n\n    Args:\n        mode (str): mode of selection\n        suffix (str): suffix to add\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.add_suffix(node, suffix))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.auto_fix_duplicates","title":"<code>auto_fix_duplicates()</code>","text":"<p>Auto rename duplicates nodes.</p> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def auto_fix_duplicates() -&gt; dict[str, str]:\n    \"\"\"Auto rename duplicates nodes.\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    nodes = get_duplicates().keys()\n\n    renamed = {}\n\n    # Rename to temp name to avoid name conflict\n    node_uuids = {}\n    for node in nodes:\n        uuid = cmds.ls(node, uuid=True)\n        if uuid:\n            node_uuids[node] = uuid[0]\n\n    for node, uuid in node_uuids.items():\n        current_name = cmds.ls(uuid, long=True)\n        if not current_name:\n            log.error(f\"Cannot find node with UUID {uuid}\")\n            continue\n\n        current_name = node\n        parent = cmds.listRelatives(current_name, parent=True, fullPath=True)\n        if parent:\n            base_name = (\n                current_name.replace(\"|\", \"_\")\n                if not current_name.startswith(\"|\")\n                else current_name.replace(\"|\", \"_\")[1:]\n            )\n\n        else:\n            base_name = current_name.split(\"|\")[-1]\n\n        new_name = renamer.renaming(base_name, 0, constants.DEFAULT_PADDING)\n\n        if new_name and new_name != current_name:\n            renamed[node] = _apply_rename(current_name, new_name, node)\n        else:\n            renamed[node] = current_name\n\n    return renamed\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.delete_workspace_control","title":"<code>delete_workspace_control(workspace_name)</code>","text":"<p>Close and delete an existing Maya workspace control.</p> <p>Parameters:</p> Name Type Description Default <code>workspace_name</code> <code>str</code> <p>name of the workspace control to delete</p> required Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def delete_workspace_control(workspace_name: str) -&gt; None:\n    \"\"\"Close and delete an existing Maya workspace control.\n\n    Args:\n        workspace_name (str): name of the workspace control to delete\n\n    \"\"\"\n    if cmds.workspaceControl(workspace_name, exists=True):\n        cmds.workspaceControl(workspace_name, edit=True, close=True)\n        cmds.deleteUI(workspace_name, control=True)\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.fix_shape_name","title":"<code>fix_shape_name()</code>","text":"<p>Rename shapes to match their transform name.</p> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: {old_name: new_name}</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def fix_shape_name() -&gt; dict[str, str]:\n    \"\"\"Rename shapes to match their transform name.\n\n    Returns:\n        dict[str, str]: {old_name: new_name}\n\n    \"\"\"\n    nodes = get_nodes(\"Scene\")\n\n    renamed = {}\n\n    for node in nodes:\n        shapes = cmds.listRelatives(node, shapes=True, fullPath=True, noIntermediate=False) or []\n        if not shapes:\n            continue\n\n        short_name = node.split(\"|\")[-1]\n\n        for index, shape in enumerate(shapes):\n            expected = renamer.fix_shape_name(short_name, index)\n\n            short_shape = shape.split(\"|\")[-1]\n\n            if shape == expected:\n                log.info(f\"{short_name} already has the correct name.\")\n                continue\n\n            renamed[short_shape] = _apply_rename(shape, expected, short_shape)\n\n    return renamed\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.get_duplicates","title":"<code>get_duplicates()</code>","text":"<p>Get duplicates nodes in the scene.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: list of duplicates nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def get_duplicates() -&gt; list[str]:\n    \"\"\"Get duplicates nodes in the scene.\n\n    Returns:\n        list[str]: list of duplicates nodes\n\n    \"\"\"\n    nice_names = {}\n    # If pipe character in selection it's a duplicate name\n    duplicates = [f for f in cmds.ls(type=\"transform\") if \"|\" in f]\n    duplicates.sort(key=lambda obj: obj.count(\"|\"), reverse=True)\n\n    cmds.select(clear=True)\n\n    if duplicates:\n        for name in duplicates:\n            # extract the base name\n            cmds.select(name, add=True)\n            m = re.compile(\"[^|]*$\").search(name)\n            shortname = m.group(0)\n            nice_names[name] = shortname\n        log.info(f\"There are {len(duplicates)} objects with duplicated name.\")\n        return nice_names\n\n    log.warning(\"No duplicates nodes\")\n    return {}\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.get_hierarchy","title":"<code>get_hierarchy()</code>","text":"<p>Get selection hierarchy.</p> <p>Returns:</p> Type Description <code>list[str] | None</code> <p>list[str]: list of selected node names with hierarchy</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def get_hierarchy() -&gt; list[str] | None:\n    \"\"\"Get selection hierarchy.\n\n    Returns:\n        list[str]: list of selected node names with hierarchy\n\n    \"\"\"\n    selected = get_selection()\n    transforms = cmds.listRelatives(selected, allDescendents=True, fullPath=True, type=\"transform\") or []\n    joints = cmds.listRelatives(selected, allDescendents=True, fullPath=True, type=\"joint\") or []\n    children = list(dict.fromkeys(transforms + joints))\n\n    return selected + children\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.get_nodes","title":"<code>get_nodes(mode)</code>","text":"<p>Get the list of nodes by the selected mode.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode selected</p> required <p>Returns:</p> Type Description <code>list[str] | None</code> <p>list[str]: list of nodes to renamed</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def get_nodes(mode) -&gt; list[str] | None:\n    \"\"\"Get the list of nodes by the selected mode.\n\n    Args:\n        mode (str): mode selected\n\n    Returns:\n        list[str]: list of nodes to renamed\n\n    \"\"\"\n    if mode == \"Selected\":\n        nodes = get_selection()\n\n    elif mode == \"Hierarchy\":\n        nodes = get_hierarchy()\n\n    elif mode == \"Scene\":\n        nodes = get_scene_objects()\n\n    else:\n        log.error(f\"Unknown mode: {mode}\")\n        return []\n\n    return nodes\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.get_scene_objects","title":"<code>get_scene_objects()</code>","text":"<p>Get all objects in the scene.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: list of all node names</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def get_scene_objects() -&gt; list[str]:\n    \"\"\"Get all objects in the scene.\n\n    Returns:\n        list[str]: list of all node names\n\n    \"\"\"\n    nodes = cmds.ls(transforms=True) or []\n\n    return [node for node in nodes if node not in constants.DEFAULT_CAMS]\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.get_selection","title":"<code>get_selection()</code>","text":"<p>Get the current selection.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list[str]</code> <p>list of selected node names</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def get_selection() -&gt; list[str]:\n    \"\"\"Get the current selection.\n\n    Returns:\n        list: list of selected node names\n\n    \"\"\"\n    selected = cmds.ls(sl=True, long=True) or []\n    if not selected:\n        log.error(\"Select at least 1 node.\")\n        raise ValueError(\"No nodes selected.\")\n    return selected\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.remove_characters","title":"<code>remove_characters(mode, position, count, from_start)</code>","text":"<p>Delete characters at given position in the node name.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>position</code> <code>int</code> <p>start index of deletion</p> required <code>count</code> <code>int</code> <p>number of characters to delete</p> required <code>from_start</code> <code>bool</code> <p>True count from start else from end.</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def remove_characters(mode, position, count, from_start) -&gt; dict[str, str] :\n    \"\"\"Delete characters at given position in the node name.\n\n    Args:\n        mode (str): mode of selection\n        position (int): start index of deletion\n        count (int): number of characters to delete\n        from_start (bool): True count from start else from end.\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.remove_characters(node, position, count, from_start))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.remove_prefix","title":"<code>remove_prefix(mode, prefix)</code>","text":"<p>Add prefix to nodes.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>prefix</code> <code>str</code> <p>prefix to remove</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def remove_prefix(mode, prefix) -&gt; dict[str, str]:\n    \"\"\"Add prefix to nodes.\n\n    Args:\n        mode (str): mode of selection\n        prefix (str): prefix to remove\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.remove_prefix(node, prefix))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.remove_suffix","title":"<code>remove_suffix(mode, suffix)</code>","text":"<p>Add suffix to nodes.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>suffix</code> <code>str</code> <p>suffix to remove</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def remove_suffix(mode, suffix) -&gt; dict[str, str]:\n    \"\"\"Add suffix to nodes.\n\n    Args:\n        mode (str): mode of selection\n        suffix (str): suffix to remove\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.remove_suffix(node, suffix))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.rename_children_from_parent","title":"<code>rename_children_from_parent(mode, padding=constants.DEFAULT_PADDING)</code>","text":"<p>Rename children from selected parents.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>padding</code> <code>int</code> <p>number of 0</p> <code>DEFAULT_PADDING</code> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def rename_children_from_parent(mode, padding=constants.DEFAULT_PADDING) -&gt; dict[str, str]:\n    \"\"\"Rename children from selected parents.\n\n    Args:\n        mode (str): mode of selection\n        padding (int): number of 0\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    parents = get_nodes(mode)\n\n    if not parents:\n        return {}\n\n    renamed = {}\n\n    for parent in parents:\n        if not cmds.objExists(parent):\n            log.error(f\"The node {parent} doesn't exist.\")\n            continue\n        children = (cmds.listRelatives(parent, children=True, fullPath=True, type=\"transform\")) or []\n        if not children:\n            log.warning(f\"{parent} has no children\")\n\n        temp_names = []\n        for child in children:\n            temp = f\"__tmp_{uuid.uuid4().hex[:8]}__\"\n            temp_name = cmds.rename(child, temp)\n            temp_names.append(temp_name)\n\n        for temp_child, index, old_child in zip(temp_names, range(1, len(children) + 1), children, strict=False):\n            short_parent = parent.split(\"|\")[-1]\n            new_name = renamer.renaming(short_parent, index, padding)\n            renamed[old_child] = _apply_rename(temp_child, new_name, old_child)\n\n    return renamed\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.rename_nodes","title":"<code>rename_nodes(mode, base_name, padding, start, step)</code>","text":"<p>Rename nodes with padding, step and start.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <code>base_name</code> <code>str</code> <p>base name</p> required <code>padding</code> <code>int</code> <p>number of 0</p> required <code>start</code> <code>int</code> <p>start number</p> required <code>step</code> <code>int</code> <p>increment between each number</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def rename_nodes(mode, base_name, padding, start, step)-&gt; dict[str, str]:\n    \"\"\"Rename nodes with padding, step and start.\n\n    Args:\n        mode (str): mode of selection\n        base_name (str): base name\n        padding (int): number of 0\n        start (int): start number\n        step (int): increment between each number\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    nodes = get_nodes(mode)\n\n    renamed = {}\n    # Range of index with step\n    numbers = range(start, start + len(nodes) * step, step)\n\n    # Rename to temp name to avoid name conflict\n    temp_names = []\n    for node in nodes:\n        temp = f\"__tmp_{uuid.uuid4().hex[:8]}__\"\n        temp_name = cmds.rename(node, temp)\n        temp_names.append(temp_name)\n\n    for node, index, old_node in zip(temp_names, numbers, nodes, strict=False):\n        if not cmds.objExists(node):\n            log.error(f\"The node {node} doesn't exist.\")\n            continue\n        new_name = renamer.renaming(base_name, index, padding)\n\n        if new_name and new_name != node:\n            renamed[old_node] = _apply_rename(node, new_name, old_node)\n        else:\n            renamed[old_node] = old_node\n\n    return renamed\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.search_replace","title":"<code>search_replace(search_name, replace_name, case)</code>","text":"<p>Search and replace name in node.</p> <p>Parameters:</p> Name Type Description Default <code>search_name</code> <code>str</code> <p>name to find</p> required <code>replace_name</code> <code>str</code> <p>new name to replace</p> required <code>case</code> <code>bool</code> <p>case sensitive</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: renamed nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def search_replace(search_name, replace_name, case) -&gt; dict[str, str]:\n    \"\"\"Search and replace name in node.\n\n    Args:\n        search_name (str): name to find\n        replace_name (str): new name to replace\n        case (bool): case sensitive\n\n\n    Returns:\n        dict[str, str]: renamed nodes\n\n    \"\"\"\n    return _process_nodes(\n        \"Scene\",\n        lambda node: renamer.search_replace(node, search_name, replace_name, case),\n    )\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.select_item","title":"<code>select_item(index, datas)</code>","text":"<p>Select item from the duplicates list.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>index from the list</p> required <code>datas</code> <code>dict</code> <p>dictionary for duplicates nodes</p> required Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def select_item(index, datas) -&gt; list[str] | None:\n    \"\"\"Select item from the duplicates list.\n\n    Args:\n        index (int): index from the list\n        datas (dict): dictionary for duplicates nodes\n\n    \"\"\"\n    if datas:\n        cmds.select(clear=True)\n        items = list(datas.keys())\n        cmds.select(items[index])\n        log.info(f\"Select {items[index]} node\")\n        return items[index]\n\n    log.warning(\"No selection\")\n    return None\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.swap_side","title":"<code>swap_side(mode, swap_sides=constants.SWAP_SIDES)</code>","text":"<p>Swap side indicator in the node.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>selected mode</p> required <code>swap_sides</code> <code>dict[str, str]</code> <p>dictionary of the sides. Defaults to constants.SWAP_SIDES.</p> <code>SWAP_SIDES</code> <p>Returns:</p> Name Type Description <code>_process_nodes</code> <code>dict[str, str]</code> <p>function to rename the node</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def swap_side(mode, swap_sides=constants.SWAP_SIDES) -&gt; dict[str, str]:\n    \"\"\"Swap side indicator in the node.\n\n    Args:\n        mode (str): selected mode\n        swap_sides (dict[str, str], optional): dictionary of the sides. Defaults to constants.SWAP_SIDES.\n\n    Returns:\n        _process_nodes: function to rename the node\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.swap_side(node, swap_sides))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.text_to_camel","title":"<code>text_to_camel(mode)</code>","text":"<p>Convert text to camel.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def text_to_camel(mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to camel.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.text_to_camel(node))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.text_to_capitalize","title":"<code>text_to_capitalize(mode)</code>","text":"<p>Convert text to capitalize.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def text_to_capitalize(mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to capitalize.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.text_to_capitalize(node))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.text_to_lowercase","title":"<code>text_to_lowercase(mode)</code>","text":"<p>Convert text to lowercase.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def text_to_lowercase(mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to lowercase.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.text_to_lowercase(node))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.text_to_pascal","title":"<code>text_to_pascal(mode)</code>","text":"<p>Convert text to pascal.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def text_to_pascal(mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to pascal.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.text_to_pascal(node))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.text_to_snake","title":"<code>text_to_snake(mode)</code>","text":"<p>Convert text to snake.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def text_to_snake(mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to snake.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.text_to_snake(node))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.text_to_title","title":"<code>text_to_title(mode)</code>","text":"<p>Convert text to title.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def text_to_title(mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to title.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.text_to_title(node))\n</code></pre>"},{"location":"API/model/maya_api/#crossrenamertool.model.maya_api.text_to_uppercase","title":"<code>text_to_uppercase(mode)</code>","text":"<p>Convert text to uppercase.</p> <p>Parameters:</p> Name Type Description Default <code>mode</code> <code>str</code> <p>mode of selection</p> required <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: dictionary of nodes</p> Source code in <code>src/crossrenamertool/model/maya_api.py</code> <pre><code>def text_to_uppercase(mode) -&gt; dict[str, str]:\n    \"\"\"Convert text to uppercase.\n\n    Args:\n        mode (str): mode of selection\n\n    Returns:\n        dict[str, str]: dictionary of nodes\n\n    \"\"\"\n    return _process_nodes(mode, lambda node: renamer.text_to_uppercase(node))\n</code></pre>"},{"location":"API/view/","title":"View","text":"<ul> <li>Blender</li> <li>Maya</li> </ul>"},{"location":"API/view/maya/","title":"Maya View","text":"<ul> <li>MainWindow</li> <li>Widgets<ul> <li>About Dialog</li> <li>Case Widget</li> <li>Insert Remove Widget</li> <li>Prefix Suffix Widget</li> <li>Preset Combobox</li> <li>Preset Dialog</li> <li>Rename Widget</li> <li>Search Replace Widget</li> <li>Selection Mode Widget</li> <li>Utils Widget</li> </ul> </li> </ul>"},{"location":"API/view/maya/main_window/","title":"MainWindow","text":""},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window-classes","title":"Classes","text":""},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow","title":"<code>MainWindow(controller, parent=None)</code>","text":"<p>               Bases: <code>MayaQWidgetDockableMixin</code>, <code>QMainWindow</code></p> <p>Main Window for the tool.</p> Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def __init__(self, controller, parent=None):\n    super().__init__(parent)\n    self.controller = controller\n\n    self.rename_page = rename_widget.RenamePage()\n    self.prefix_suffix_page = prefix_suffix_widget.PrefixSuffixPage()\n    self.search_replace_page = search_replace_widget.SearchReplacePage()\n    self.insert_remove_page = insert_remove_widget.InsertRemovePage()\n    self.case_page = case_widget.CasePage()\n    self.utils_page = utils_widget.UtilsPage()\n    self.mode_bar = selection_mode_widget.SelectionModeBar()\n    self.datas = {}\n\n    self._configure()\n    self._create_gui()\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow-functions","title":"Methods:","text":""},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.add_prefix","title":"<code>add_prefix(prefix)</code>","text":"<p>Add prefix to nodes.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>str</code> <p>prefix. Defaults to \"\".</p> required Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def add_prefix(self, prefix):\n    \"\"\"Add prefix to nodes.\n\n    Args:\n        prefix (str, optional): prefix. Defaults to \"\".\n\n    \"\"\"\n    self.controller.add_prefix(self.mode_bar.mode, prefix)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.add_suffix","title":"<code>add_suffix(suffix)</code>","text":"<p>Add suffix to nodes.</p> <p>Parameters:</p> Name Type Description Default <code>suffix</code> <code>str</code> <p>suffix to add. Defaults to \"\".</p> required Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def add_suffix(self, suffix):\n    \"\"\"Add suffix to nodes.\n\n    Args:\n        suffix (str, optional): suffix to add. Defaults to \"\".\n\n    \"\"\"\n    self.controller.add_suffix(self.mode_bar.mode, suffix)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.insert_characters","title":"<code>insert_characters(text, position, from_start)</code>","text":"<p>Add characters to a text at a specific position by the start.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>characters to add</p> required <code>position</code> <code>int</code> <p>position to insert</p> required <code>from_start</code> <code>bool</code> <p>True from start of name otherwise False</p> required Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def insert_characters(self, text, position, from_start):\n    \"\"\"Add characters to a text at a specific position by the start.\n\n    Args:\n        text (str): characters to add\n        position (int): position to insert\n        from_start (bool): True from start of name otherwise False\n\n    \"\"\"\n    self.controller.add_characters(mode=self.mode_bar.mode, text=text, position=position, from_start=from_start)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.launch_app","title":"<code>launch_app()</code>","text":"<p>Launch the application.</p> Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def launch_app(self):\n    \"\"\"Launch the application.\"\"\"\n    self.show(dockable=True)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.load_qss","title":"<code>load_qss()</code>","text":"<p>Load the Qss file who contains the style of the application.</p> Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def load_qss(self):\n    \"\"\"Load the Qss file who contains the style of the application.\"\"\"\n    qss_path = Path(__file__).parent.parent / \"styles\" / \"default_style.qss\"\n\n    with open(qss_path, encoding=\"utf-8\") as qss_file:\n        style = qss_file.read()\n\n    style = style.replace(\"{ICONS_DIR}\", self.ICONS_PATH.as_posix())\n\n    return style\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.load_style","title":"<code>load_style()</code>","text":"<p>Load the style from the QSS file.</p> Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def load_style(self):\n    \"\"\"Load the style from the QSS file.\"\"\"\n    style = self.load_qss()\n    self.setStyleSheet(style)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.remove_characters","title":"<code>remove_characters(count, position, from_start)</code>","text":"<p>Remove characters to a text at a specific position by the start.</p> <p>Parameters:</p> Name Type Description Default <code>count</code> <code>int</code> <p>number of characters to remove</p> required <code>position</code> <code>int</code> <p>position to remove</p> required <code>from_start</code> <code>bool</code> <p>True from start of name otherwise False</p> required Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def remove_characters(self, count, position, from_start):\n    \"\"\"Remove characters to a text at a specific position by the start.\n\n    Args:\n        count (int): number of characters to remove\n        position (int): position to remove\n        from_start (bool): True from start of name otherwise False\n\n    \"\"\"\n    self.controller.remove_characters(\n        mode=self.mode_bar.mode,\n        position=position,\n        count=count,\n        from_start=from_start,\n    )\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.remove_prefix","title":"<code>remove_prefix(prefix)</code>","text":"<p>Remove prefix to nodes.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>str</code> <p>prefix. Defaults to \"\".</p> required Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def remove_prefix(self, prefix):\n    \"\"\"Remove prefix to nodes.\n\n    Args:\n        prefix (str, optional): prefix. Defaults to \"\".\n\n    \"\"\"\n    self.controller.remove_prefix(self.mode_bar.mode, prefix)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.remove_suffix","title":"<code>remove_suffix(suffix)</code>","text":"<p>Remove suffix to nodes.</p> <p>Parameters:</p> Name Type Description Default <code>suffix</code> <code>str</code> <p>suffix. Defaults to \"\".</p> required Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def remove_suffix(self, suffix):\n    \"\"\"Remove suffix to nodes.\n\n    Args:\n        suffix (str, optional): suffix. Defaults to \"\".\n\n    \"\"\"\n    self.controller.remove_suffix(self.mode_bar.mode, suffix)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.rename_nodes","title":"<code>rename_nodes(name, padding, start, step)</code>","text":"<p>Rename selected nodes.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>base name. Defaults to \"\".</p> required <code>padding</code> <code>int</code> <p>number of 0. Defaults to 3.</p> required <code>start</code> <code>int</code> <p>start number. Defaults to 1.</p> required <code>step</code> <code>int</code> <p>increment between each number. Defaults to 1.</p> required Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def rename_nodes(self, name, padding, start, step):\n    \"\"\"Rename selected nodes.\n\n    Args:\n        name (str, optional): base name. Defaults to \"\".\n        padding (int, optional): number of 0. Defaults to 3.\n        start (int, optional): start number. Defaults to 1.\n        step (int, optional): increment between each number. Defaults to 1.\n\n    \"\"\"\n    self.controller.rename_nodes(self.mode_bar.mode, name, padding, start, step)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.search_replace","title":"<code>search_replace(search_text, replace_text, case)</code>","text":"<p>Search and replace name in node.</p> <p>Parameters:</p> Name Type Description Default <code>search_text</code> <code>str</code> <p>name to find</p> required <code>replace_text</code> <code>str</code> <p>new name to replace</p> required <code>case</code> <code>bool</code> <p>case sensitive</p> required Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def search_replace(self, search_text, replace_text, case):\n    \"\"\"Search and replace name in node.\n\n    Args:\n        search_text (str): name to find\n        replace_text (str): new name to replace\n        case (bool): case sensitive\n\n    \"\"\"\n    self.controller.search_replace(search_text, replace_text, case)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.text_to_camel","title":"<code>text_to_camel()</code>","text":"<p>Convert text to camel.</p> Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def text_to_camel(self):\n    \"\"\"Convert text to camel.\"\"\"\n    self.controller.text_to_camel(self.mode_bar.mode)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.text_to_capitalize","title":"<code>text_to_capitalize()</code>","text":"<p>Convert text to capitalize.</p> Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def text_to_capitalize(self):\n    \"\"\"Convert text to capitalize.\"\"\"\n    self.controller.text_to_capitalize(self.mode_bar.mode)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.text_to_lowercase","title":"<code>text_to_lowercase()</code>","text":"<p>Convert text to lowercase.</p> Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def text_to_lowercase(self):\n    \"\"\"Convert text to lowercase.\"\"\"\n    self.controller.text_to_lowercase(self.mode_bar.mode)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.text_to_pascal","title":"<code>text_to_pascal()</code>","text":"<p>Convert text to pascal.</p> Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def text_to_pascal(self):\n    \"\"\"Convert text to pascal.\"\"\"\n    self.controller.text_to_pascal(self.mode_bar.mode)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.text_to_snake","title":"<code>text_to_snake()</code>","text":"<p>Convert text to snake.</p> Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def text_to_snake(self):\n    \"\"\"Convert text to snake.\"\"\"\n    self.controller.text_to_snake(self.mode_bar.mode)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.text_to_title","title":"<code>text_to_title()</code>","text":"<p>Convert text to title.</p> Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def text_to_title(self):\n    \"\"\"Convert text to title.\"\"\"\n    self.controller.text_to_title(self.mode_bar.mode)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.MainWindow.text_to_uppercase","title":"<code>text_to_uppercase()</code>","text":"<p>Convert text to uppercase.</p> Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def text_to_uppercase(self):\n    \"\"\"Convert text to uppercase.\"\"\"\n    self.controller.text_to_uppercase(self.mode_bar.mode)\n</code></pre>"},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window-functions","title":"Functions:","text":""},{"location":"API/view/maya/main_window/#crossrenamertool.ui.maya.main_window.create_view","title":"<code>create_view(controller)</code>","text":"<p>Create the view of the module.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>CrossRenamerToolController</code> <p>controller of the module.</p> required Source code in <code>src/crossrenamertool/ui/maya/main_window.py</code> <pre><code>def create_view(controller):\n    \"\"\"Create the view of the module.\n\n    Args:\n        controller (CrossRenamerToolController): controller of the module.\n\n    \"\"\"\n    workspace_name = MainWindow.OBJECT_NAME + \"WorkspaceControl\"\n    controller.delete_workspace_control(workspace_name)\n\n    try:\n        view = MainWindow(controller=controller)\n        log.info(\"MainWindow created successfully\")\n        return view\n    except Exception as e:\n        log.error(f\"Failed to create MainWindow: {e}\")\n        controller.delete_workspace_control(workspace_name)\n        raise\n</code></pre>"},{"location":"API/view/maya/widgets/","title":"Widgets","text":"<ul> <li>About Dialog</li> <li>Case Widget</li> <li>Insert Remove Widget</li> <li>Prefix Suffix Widget</li> <li>Preset Combobox</li> <li>Preset Dialog</li> <li>Rename Widget</li> <li>Search Replace Widget</li> <li>Selection Mode Widget</li> <li>Utils Widget</li> </ul>"},{"location":"API/view/maya/widgets/about_dialog/","title":"About Dialog","text":""},{"location":"API/view/maya/widgets/about_dialog/#crossrenamertool.ui.maya.widgets.about_dialog-classes","title":"Classes","text":""},{"location":"API/view/maya/widgets/about_dialog/#crossrenamertool.ui.maya.widgets.about_dialog.AboutDialog","title":"<code>AboutDialog(parent=None, stylesheet='')</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>About page with documentation and GitHub page.</p> Source code in <code>src/crossrenamertool/ui/maya/widgets/about_dialog.py</code> <pre><code>def __init__(self, parent=None, stylesheet=\"\"):\n    super().__init__(parent)\n\n    self.stylesheet = stylesheet\n\n    self._configure()\n    self._create_gui()\n</code></pre>"},{"location":"API/view/maya/widgets/case_widget/","title":"Case Widget","text":""},{"location":"API/view/maya/widgets/case_widget/#crossrenamertool.ui.maya.widgets.case_widget-classes","title":"Classes","text":""},{"location":"API/view/maya/widgets/case_widget/#crossrenamertool.ui.maya.widgets.case_widget.CasePage","title":"<code>CasePage(parent=None)</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget for case sensitive page.</p> <p>Initialize the widget.</p> Source code in <code>src/crossrenamertool/ui/maya/widgets/case_widget.py</code> <pre><code>def __init__(self, parent=None) -&gt; None:\n    \"\"\"Initialize the widget.\"\"\"\n    super().__init__(parent=parent)\n\n    self._configure()\n    self._create_gui()\n</code></pre>"},{"location":"API/view/maya/widgets/case_widget/#crossrenamertool.ui.maya.widgets.case_widget.CasePage-functions","title":"Methods:","text":""},{"location":"API/view/maya/widgets/insert_remove_widget/","title":"Insert Remove Widget","text":""},{"location":"API/view/maya/widgets/insert_remove_widget/#crossrenamertool.ui.maya.widgets.insert_remove_widget-classes","title":"Classes","text":""},{"location":"API/view/maya/widgets/insert_remove_widget/#crossrenamertool.ui.maya.widgets.insert_remove_widget.InsertRemovePage","title":"<code>InsertRemovePage(parent=None)</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget for insert and remove page.</p> <p>Initialize the widget.</p> Source code in <code>src/crossrenamertool/ui/maya/widgets/insert_remove_widget.py</code> <pre><code>def __init__(self, parent=None) -&gt; None:\n    \"\"\"Initialize the widget.\"\"\"\n    super().__init__(parent=parent)\n\n    self._configure()\n    self._create_gui()\n</code></pre>"},{"location":"API/view/maya/widgets/insert_remove_widget/#crossrenamertool.ui.maya.widgets.insert_remove_widget.InsertRemovePage-functions","title":"Methods:","text":""},{"location":"API/view/maya/widgets/prefix_suffix_widget/","title":"Prefix Suffix Widget","text":""},{"location":"API/view/maya/widgets/prefix_suffix_widget/#crossrenamertool.ui.maya.widgets.prefix_suffix_widget-classes","title":"Classes","text":""},{"location":"API/view/maya/widgets/prefix_suffix_widget/#crossrenamertool.ui.maya.widgets.prefix_suffix_widget.PrefixSuffixPage","title":"<code>PrefixSuffixPage(parent=None)</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget for prefix and suffix options.</p> <p>Initialize the widget.</p> Source code in <code>src/crossrenamertool/ui/maya/widgets/prefix_suffix_widget.py</code> <pre><code>def __init__(self, parent=None) -&gt; None:\n    \"\"\"Initialize the widget.\"\"\"\n    super().__init__(parent=parent)\n\n    self._configure()\n    self._create_gui()\n</code></pre>"},{"location":"API/view/maya/widgets/prefix_suffix_widget/#crossrenamertool.ui.maya.widgets.prefix_suffix_widget.PrefixSuffixPage-functions","title":"Methods:","text":""},{"location":"API/view/maya/widgets/preset_combobox/","title":"Preset Combobox","text":""},{"location":"API/view/maya/widgets/preset_combobox/#crossrenamertool.ui.maya.widgets.preset_combobox-classes","title":"Classes","text":""},{"location":"API/view/maya/widgets/preset_combobox/#crossrenamertool.ui.maya.widgets.preset_combobox.PresetComboBox","title":"<code>PresetComboBox</code>","text":"<p>               Bases: <code>QComboBox</code></p> <p>ComboBox with right-click context menu for preset management.</p> Signals <p>request_save   (str) : emitted when user wants to save current text as preset request_remove (str) : emitted when user wants to remove current text from presets request_manage ()    : emitted when user wants to open the preset manager dialog</p>"},{"location":"API/view/maya/widgets/preset_combobox/#crossrenamertool.ui.maya.widgets.preset_combobox.PresetComboBox-functions","title":"Methods:","text":""},{"location":"API/view/maya/widgets/preset_combobox/#crossrenamertool.ui.maya.widgets.preset_combobox.PresetComboBox.contextMenuEvent","title":"<code>contextMenuEvent(event)</code>","text":"<p>Show context menu on right click.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>QContextMenuEvent</code> <p>mouse event</p> required Source code in <code>src/crossrenamertool/ui/maya/widgets/preset_combobox.py</code> <pre><code>def contextMenuEvent(self, event):\n    \"\"\"Show context menu on right click.\n\n    Args:\n        event (QContextMenuEvent): mouse event\n\n    \"\"\"\n    menu = QtWidgets.QMenu(self)\n\n    current = self.currentText().strip()\n\n    save_action = menu.addAction(\"Save as preset\")\n    save_action.setEnabled(bool(current))\n    save_action.setToolTip(\"Save the current text as a reusable preset.\")\n\n    remove_action = menu.addAction(\"X Remove from presets\")\n    remove_action.setEnabled(bool(current))\n    remove_action.setToolTip(\"Remove the selected value from the preset list.\")\n\n    menu.addSeparator()\n\n    manage_action = menu.addAction(\"Manage presets...\")\n    manage_action.setToolTip(\"Open the preset manager to add, remove or reorder presets.\")\n\n    action = menu.exec(event.globalPos())\n\n    if action == save_action:\n        self.request_save.emit(current)\n    elif action == remove_action:\n        self.request_remove.emit(current)\n    elif action == manage_action:\n        self.request_manage.emit()\n</code></pre>"},{"location":"API/view/maya/widgets/preset_combobox/#crossrenamertool.ui.maya.widgets.preset_combobox.PresetComboBox.reload","title":"<code>reload(items)</code>","text":"<p>Reload the combobox with a new list of presets.</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>list[str]</code> <p>new list of presets</p> required Source code in <code>src/crossrenamertool/ui/maya/widgets/preset_combobox.py</code> <pre><code>def reload(self, items):\n    \"\"\"Reload the combobox with a new list of presets.\n\n    Args:\n        items (list[str]): new list of presets\n\n    \"\"\"\n    current = self.currentText()\n\n    self.blockSignals(True)\n    self.clear()\n    self.addItems(items)\n    self.blockSignals(False)\n\n    index = self.findText(current)\n    self.setCurrentIndex(index if index &gt;= 0 else -1)\n    return current\n</code></pre>"},{"location":"API/view/maya/widgets/preset_dialog/","title":"Preset Dialog","text":""},{"location":"API/view/maya/widgets/preset_dialog/#crossrenamertool.ui.maya.widgets.preset_dialog-classes","title":"Classes","text":""},{"location":"API/view/maya/widgets/preset_dialog/#crossrenamertool.ui.maya.widgets.preset_dialog.PresetsDialog","title":"<code>PresetsDialog(parent=None, stylesheet='')</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Dialog for managing prefix and suffis presets.</p> Source code in <code>src/crossrenamertool/ui/maya/widgets/preset_dialog.py</code> <pre><code>def __init__(self, parent=None, stylesheet=\"\"):\n    super().__init__(parent)\n\n    self.stylesheet = stylesheet\n\n    self._configure()\n    self._create_gui()\n    self._load()\n</code></pre>"},{"location":"API/view/maya/widgets/rename_widget/","title":"Rename Widget","text":""},{"location":"API/view/maya/widgets/rename_widget/#crossrenamertool.ui.maya.widgets.rename_widget-classes","title":"Classes","text":""},{"location":"API/view/maya/widgets/rename_widget/#crossrenamertool.ui.maya.widgets.rename_widget.RenamePage","title":"<code>RenamePage(parent=None)</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget for rename nodes options.</p> <p>Initialize the widget.</p> Source code in <code>src/crossrenamertool/ui/maya/widgets/rename_widget.py</code> <pre><code>def __init__(self, parent=None):\n    \"\"\"Initialize the widget.\"\"\"\n    super().__init__(parent=parent)\n\n    self._configure()\n    self._create_gui()\n</code></pre>"},{"location":"API/view/maya/widgets/rename_widget/#crossrenamertool.ui.maya.widgets.rename_widget.RenamePage-functions","title":"Methods:","text":""},{"location":"API/view/maya/widgets/search_replace_widget/","title":"Search Replace Widget","text":""},{"location":"API/view/maya/widgets/search_replace_widget/#crossrenamertool.ui.maya.widgets.search_replace_widget-classes","title":"Classes","text":""},{"location":"API/view/maya/widgets/search_replace_widget/#crossrenamertool.ui.maya.widgets.search_replace_widget.SearchReplacePage","title":"<code>SearchReplacePage(parent=None)</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget for search and replace nodes options.</p> <p>Initialize the widget.</p> Source code in <code>src/crossrenamertool/ui/maya/widgets/search_replace_widget.py</code> <pre><code>def __init__(self, parent=None) -&gt; None:\n    \"\"\"Initialize the widget.\"\"\"\n    super().__init__(parent=parent)\n\n    self._configure()\n    self._create_gui()\n</code></pre>"},{"location":"API/view/maya/widgets/search_replace_widget/#crossrenamertool.ui.maya.widgets.search_replace_widget.SearchReplacePage-functions","title":"Methods:","text":""},{"location":"API/view/maya/widgets/selection_mode_widget/","title":"Selection Mode Widget","text":""},{"location":"API/view/maya/widgets/selection_mode_widget/#crossrenamertool.ui.maya.widgets.selection_mode_widget-classes","title":"Classes","text":""},{"location":"API/view/maya/widgets/selection_mode_widget/#crossrenamertool.ui.maya.widgets.selection_mode_widget.SelectionModeBar","title":"<code>SelectionModeBar(parent=None)</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget for insert and remove page.</p> <p>Initialize the widget.</p> Source code in <code>src/crossrenamertool/ui/maya/widgets/selection_mode_widget.py</code> <pre><code>def __init__(self, parent=None):\n    \"\"\"Initialize the widget.\"\"\"\n    super().__init__(parent=parent)\n\n    self._configure()\n    self._create_gui()\n</code></pre>"},{"location":"API/view/maya/widgets/selection_mode_widget/#crossrenamertool.ui.maya.widgets.selection_mode_widget.SelectionModeBar-functions","title":"Methods:","text":""},{"location":"API/view/maya/widgets/utils_widget/","title":"Utils Widget","text":""},{"location":"API/view/maya/widgets/utils_widget/#crossrenamertool.ui.maya.widgets.utils_widget-classes","title":"Classes","text":""},{"location":"API/view/maya/widgets/utils_widget/#crossrenamertool.ui.maya.widgets.utils_widget.UtilsPage","title":"<code>UtilsPage(parent=None)</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget for rename nodes options.</p> <p>Initialize the widget.</p> Source code in <code>src/crossrenamertool/ui/maya/widgets/utils_widget.py</code> <pre><code>def __init__(self, parent=None) -&gt; None:\n    \"\"\"Initialize the widget.\"\"\"\n    super().__init__(parent=parent)\n\n    self._configure()\n    self._create_gui()\n</code></pre>"},{"location":"API/view/maya/widgets/utils_widget/#crossrenamertool.ui.maya.widgets.utils_widget.UtilsPage-functions","title":"Methods:","text":""},{"location":"API/view/maya/widgets/utils_widget/#crossrenamertool.ui.maya.widgets.utils_widget.UtilsPage.ingest_list","title":"<code>ingest_list(duplicates, filters=None)</code>","text":"<p>Ingest playblast in the list.</p> <p>Parameters:</p> Name Type Description Default <code>filters</code> <code>dict</code> <p>text for filter the list. Defaults to None.</p> <code>None</code> Source code in <code>src/crossrenamertool/ui/maya/widgets/utils_widget.py</code> <pre><code>def ingest_list(self, duplicates, filters: dict | None = None) -&gt; None:\n    \"\"\"Ingest playblast in the list.\n\n    Args:\n        filters (dict, optional): text for filter the list. Defaults to None.\n\n    \"\"\"\n    self.duplicates_table.setRowCount(0)\n\n    for data, short_name in duplicates.items():\n        row = self.duplicates_table.rowCount()\n        self.duplicates_table.insertRow(row)\n\n        item_current = QtWidgets.QTableWidgetItem(data)\n        item_current.setFlags(QtCore.Qt.ItemIsSelectable | QtCore.Qt.ItemIsEnabled)\n        self.duplicates_table.setItem(row, 0, item_current)\n\n        item_new = QtWidgets.QTableWidgetItem(short_name)\n        item_new.setFlags(\n            QtCore.Qt.ItemIsSelectable | QtCore.Qt.ItemIsEnabled | QtCore.Qt.ItemIsEditable  # \u00e9ditable\n        )\n        self.duplicates_table.setItem(row, 1, item_new)\n</code></pre>"},{"location":"developer_docs/","title":"Developer Docs","text":"<p>Tutorials How-to Guides References Explanations</p>"},{"location":"developer_docs/references/","title":"References","text":"<p>The References section precisely describes the commands, parameters, or possible interactions within this tool.</p> <p>Architecture </p>"},{"location":"developer_docs/references/architecture/","title":"Architecture","text":""},{"location":"developer_docs/references/architecture/#overview","title":"Overview","text":"<p>Cross Renamer Tool follows a Model-View-Controller (MVC) pattern, with a strict separation between the renaming logic, the Maya scene operations, and the UI.</p> <p>graph TD     UI[\"UI Widget (Pyside6)\"]     MW[\"MainWindowBase UI\"]     CT[\"main.py Controller\"]     API[\"maya_api.py Maya Adapter\"]     RN[\"renamer.py Pure Logic\"]     MS[\"Maya Scene (cmds)\"]      UI --&gt;|\"Qt Signals\"| MW     MW --&gt;|\"Controller connection\"| CT     CT --&gt;|\"method calls\"| API     API --&gt;|\"pure functions\"| RN     API --&gt;|\"cmds calls\"| MS  Each layer has a single responsability and communicates only with its immediate neighbour - a UI widget never calls <code>cmds</code> directly, and <code>renamer.py</code> never imports Maya.</p>"},{"location":"developer_docs/references/architecture/#layer-breakdown","title":"Layer Breakdown","text":""},{"location":"developer_docs/references/architecture/#layer-1-pure-logic-corerenamerpy","title":"Layer 1 - Pure Logic (<code>core/renamer.py</code>)","text":"<p>Contains all renaming functions pure Python functions. No Maya dependency, no side effects - a function takes a string and returns aa string.</p> <p><pre><code># renamer.py - pure function, No DCC\ndef add_prefix(base_name: str, prefix: str) -&gt; str:\n    return f\"{prefix}_{base_name}\"\n</code></pre> This layer is fully unit-testable without launching Maya. Every renaming operation - padding, search/replace, swap side, fix shape name - lives here.</p>"},{"location":"developer_docs/references/architecture/#layer-2-maya-adapter-modelmaya_apipy","title":"Layer 2 - Maya Adapter (<code>model/maya_api.py</code>)","text":"<p>Bridges the pure logic and the Maya scene. It: - retrieves nodes from the scene via <code>cmds.ms</code>, <code>cmds.listRelatives</code> - calls <code>renamer.py</code> for the actual name transformations - applies the result with <code>cmds.rename</code> - logs warning and errors</p> <p><pre><code># maya_api.py - calls renamer.py then cmds\ndef add_prefix(mode: str, prefix: str) -&gt; dict:\n    return _process_nodes(mode, lambda node: renamer.add_prefix(node, prefix))\n</code></pre> The <code>_process_nodes</code> helper centralises the node retrieval + rename + log pattern - all operations use it instead of duplicating the loop.</p>"},{"location":"developer_docs/references/architecture/#layer-3-controller-mainpy","title":"Layer 3 - Controller (<code>main.py</code>)","text":"<p><code>CrossRenamerToolController</code> is the single entry point between the UI and the DCC Adapter. It : - receives calls from <code>MainWindow</code> - delegates to <code>maya_api.py</code> - never contains business logic</p> <pre><code># main.py - delegates only, no logic\ndef add_prefix(self, mode: str, prefix : str)-&gt; dict:\n    return maya_api.add_prefix(mode=mode, prefix=prefix)\n</code></pre>"},{"location":"developer_docs/references/architecture/#layer-4-ui-uimaya","title":"Layer 4 - UI (<code>UI/maya/</code>)","text":"<p>Each features is a standalone <code>QWidget</code> that communicates via QtSignals only - no direct calls to the controller or DCC.</p> <p><pre><code># prefix_suffix_widget.py - emits a signal, nothing else\ndef _on_add_prefix(self) -&gt; None:\n    prefix = self.prefix.currentText()\n    if prefix:\n        self.request_prefix.emit(prefix)\n</code></pre> <code>MainWindow</code> connects all signals to the controller methods and handles the mode selector.</p>"},{"location":"developer_docs/references/architecture/#file-structure","title":"File Structure","text":"<pre><code>\ud83d\udcc2Project_Root\n \u251c\u2500\ud83d\udcc4 .env\n \u251c\u2500\ud83d\udeab .gitignore\n \u251c\u2500\ud83d\udcc4 CHANGELOG.md\n \u251c\u2500\ud83d\udcc4 install.py\n \u251c\u2500\ud83d\udcc4 LICENSE\n \u251c\u2500\ud83d\udcc4 mkdocs.yml\n \u251c\u2500\ud83d\udcc4 pyproject.toml\n \u251c\u2500\ud83d\udcc4 README.md\n \u251c\u2500\ud83d\udcc4 requirements.txt\n \u251c\u2500\ud83d\udcc4 .env\n \u2514\ud83d\udcc2src\n  \u251c\u2500\ud83d\udcc2crossrenamertool\n  \u2502  \u251c\u2500\ud83d\udcc2core\n  \u2502  \u2502  \u251c\u2500\ud83d\udcc4__init__.py\n  \u2502  \u2502  \u251c\u2500\ud83d\udcc4constants.py\n  \u2502  \u2502  \u251c\u2500\ud83d\udcc4presets.py\n  \u2502  \u2502  \u2514\u2500\ud83d\udcc4renamer.py\n  \u2502  \u251c\u2500\ud83d\udcc2model\n  \u2502  \u2502  \u251c\u2500\ud83d\udcc4__init__.py\n  \u2502  \u2502  \u2514\u2500\ud83d\udcc4maya_api.py\n  \u2502  \u251c\u2500\ud83d\udcc2resources\n  \u2502  \u2502  \u251c\u2500\ud83d\udcc2icons\n  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\uddbc\ufe0fdown_arrow.svg\n  \u2502  \u2502  \u2502  \u2514\u2500\ud83d\uddbc\ufe0fup_arrow.svg\n  \u2502  \u2502  \u251c\u2500\ud83d\udcc2presets\n  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc4prefixes.json\n  \u2502  \u2502  \u2502  \u2514\u2500\ud83d\udcc4suffixes.json\n  \u2502  \u2502  \u2514\u2500\ud83d\udcc4README.md\n  \u2502  \u251c\u2500\ud83d\udcc2ui\n  \u2502  \u2502  \u251c\u2500\ud83d\udcc2blender\n  \u2502  \u2502  \u2502  \u2514\u2500\ud83d\udcc4__init__.py\n  \u2502  \u2502  \u251c\u2500\ud83d\udcc2maya\n  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc2widget\n  \u2502  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc4__init__.py\n  \u2502  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc4about_dialog.py\n  \u2502  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc4case_widget.py\n  \u2502  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc4insert_remove_widget.py\n  \u2502  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc4prefix_suffix_widget.py\n  \u2502  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc4preset_combobox.py\n  \u2502  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc4preset_dialog.py\n  \u2502  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc4rename_widget.py\n  \u2502  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc4search_replace_widget.py\n  \u2502  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc4selection_mode_widget.py\n  \u2502  \u2502  \u2502  \u2502  \u2514\u2500\ud83d\udcc4utils_widget.py\n  \u2502  \u2502  \u2502  \u251c\u2500\ud83d\udcc4__init__.py\n  \u2502  \u2502  \u2502  \u2514\u2500\ud83d\udcc4main_window.py \n  \u2502  \u2502  \u251c\u2500\ud83d\udcc2styles\n  \u2502  \u2502  \u2502  \u251c\u2500\ud83c\udfa8default_style.qss\n  \u2502  \u2502  \u2502  \u2514\u2500\ud83d\udcc4__init__.py\n  \u2502  \u2502  \u251c\u2500\ud83d\udcc4__init__.py\n  \u2502  \u2502  \u251c\u2500\ud83d\udcc4launcher.py\n  \u2502  \u2502  \u2514\u2500\ud83d\udcc4main.py\n  \u2502  \u2514\u2500\ud83d\udcc4__init__.py\n  \u251c\u2500\ud83d\udcc2tests\n  \u2502  \u251c\u2500\ud83d\udcc2test_cross_renamer_tool\n  \u2502  \u2502  \u251c\u2500\ud83d\udcc4__init__.py\n  \u2502  \u2502  \u2514\u2500\ud83d\udcc4test_renaming.py\n  \u2502  \u251c\u2500\ud83d\udcc4__init__.py\n  \u2502  \u2514\u2500\ud83d\udcc4README.md\n  \u251c\u2500\ud83d\udcc2bin\n  \u2502  \u2514\u2500\ud83d\udcc4README.md\n  \u251c\u2500\ud83d\udcc2data\n  \u2502  \u2514\u2500\ud83d\udcc4README.md\n  \u2514\u2500\ud83d\udcc2docs\n     \u2514\u2500\ud83d\udcc2etc../\n</code></pre>"},{"location":"developer_docs/references/architecture/#selection-modes","title":"Selection Modes","text":"<p>Node retrieval is centralised in <code>get_nodes(mode)</code> in  <code>maya_api.py</code>. three modes are supported:  </p> Mode Implementation Notes <code>Selected</code> <code>cmds.ls(sl=True)</code> Current Maya selection <code>Hierarchy</code> <code>cmds.listRelatives(allDescendents=True, type=\"transform\")</code> + joints Shapes excluded <code>Scene</code> <code>cmds.ls(transforms=True)</code> Default cameras excluded via <code>constants.DEFAULT_CAMS</code>"},{"location":"developer_docs/references/architecture/#double-pass-renaming","title":"Double-Pass Renaming","text":"<p>To avoid name conflicts when renaming multiples nodes (e.g. ith step or reordering), all batch rename operations ise a two-pass  strategy: <pre><code>Pass 1 - rename every node to a unique temporary name\n         using uuid4 -&gt; __tmp_a3f9c21b__\nPass 2 - rename every temporary name its final name\n</code></pre> This prevents Maya from auto-incrementing names when a target name already exists in the scene.</p> <p>See Explanations: Why to-Pass Renaming</p>"},{"location":"developer_docs/references/architecture/#preset-system","title":"Preset System","text":"<p>Presets are stored as JSON files in <code>resources/presets/</code> and managed by <code>core/presets.py</code>.</p> <p><pre><code>load_presets(type)  -&gt;  reads JSON\nsave_presets(type)  -&gt;  writes JSON\nadd_preset(type)    -&gt;  load -&gt; append -&gt; save\nremove_preset(type) -&gt;  load -&gt; remove -&gt; save\n</code></pre> The UI exposes presets via <code>PresetComboBox</code> - a custom <code>QComboBox</code> with a right-click context menu - and <code>PresetDialog</code> for full management (add, remove, reorder, rename, inline, reset to defaults).</p>"},{"location":"developer_docs/references/design_choices/","title":"Design Choices","text":"<p>This page explains the UX and architectural decision made during development, and the reasoning behind each one.</p>"},{"location":"developer_docs/references/design_choices/#ui-qstackedwidget-over-collapsible-sections","title":"UI - QStackedWidget over collapsible sections","text":"<p>Decisions: Use a tab-based navigation (<code>QStackedWidget</code> + custom nav buttons) instead of collapsible sections. Why: A renaming tool has many features - Rename, Prefix/Suffix, Search/Replace, Insert/Remove, Convert Case, Utils. Whit collapsible sections, all of them would be visible at once, requiring the artist to scroll and mentally parse what is expanded and what is not. With <code>QStackedWidget</code>, only one page is visible at a time. The UI stays compact and the artist focuses on one task. The window height stays fixed regardless of which page is open. Trade-off: Features are not immediately all visible - the artist needs to know which tab to click. This is acceptable because the tab labels are explicit and the features are logically grouped.</p>"},{"location":"developer_docs/references/design_choices/#ui-global-mode-selector-always-visible","title":"UI - GLobal mode selector always visible","text":"<p>Decision: The Apply on selector (Selected/Hierarchy/Scene) is fixed at the top of the window, outside the tab pages. Why: The mode applies to every operation - Rename, Prefix/Suffix, etc. If it lived inside each page, the artist would to set it repeatedly. Keeping it global means setting it once and forgetting it. Trade-off: The mode is shared across all operations - changing it affects the next action regardless of which page is active. This is documented in the tooltips.</p>"},{"location":"developer_docs/references/design_choices/#ui-preset-manager-as-dialog-not-inline","title":"UI -  Preset Manager as dialog, not inline","text":"<p>Decision: Preset management (add, remove, reorder, rename) lives in a dedicated <code>PresetDialog</code>, not inline in the <code>PrefixSuffixPage</code>. Why: Managing presets is a rare action - it happens once when setting up the tool, not on every rename. Inline management (a list with add/remove buttons next to the combobox) would clutter the daily-use UI with controls that are almost never needed. The dialog keeps the main UI clean. The right-click context menu on the combobox provides a fast path for the most common actions (save/remove) without opening the dialog.</p>"},{"location":"developer_docs/references/design_choices/#architecture-pure-functions-in-renamerpy","title":"Architecture - Pure functions in <code>renamer.py</code>","text":"<p>Decision: All renaming logic lives in <code>renamer.py</code> as pure Python functions with no DCC dependency. Why: - Testability - unit tests run without a DCC session. This is critical for CI/CD. - Reusability - the same logic could be reused in an other DCC adapter or a standalone CLI tool. - Clarity -  a function that takes a string and returns a string is easy to reason about and debug.  </p> <p>Trade-off: An extra layer of indirection - <code>maya_api.py</code> calls <code>renamer.py</code> which does the work. Accepted because the separation pays off in testability and futur portability.</p>"},{"location":"developer_docs/references/design_choices/#architecture-_process_nodes-helper","title":"Architecture - <code>_process_nodes</code> helper","text":"<p>Decision: All node-level operations in <code>maya_api.py</code> go through a single <code>_process_nodes(mode, transform_fn)</code> helper instead of each function having its own loop. Why: Without <code>_process_nodes</code>, every function (<code>add_prefix</code>, <code>add_suffix</code>, <code>text_to_lowercase</code>...) would repeat the same pattern: <pre><code>nodes = get_nodes(mode)\nfor node in nodes:\n    if not cmds.objExists(node):\n        log.error(...)\n        continue\n    new_name = some_function(node)\n    _apply_rename(node, new_name)\n</code></pre> With <code>_process_nodes</code>, each operation becomes a one-liner: <pre><code>def add_prefix(mode: str, prefix: str) -&gt; dict[str, str]:\n    return _process_nodes(mode, lambda node: renamer.add_prefix(node, prefix))\n</code></pre> Trade-off: Operations that need special handling (batch rename with step, fix duplicates) cannot use <code>_process_nodes</code> and implement their own loop. This is intentional - <code>_process_nodes</code> is for the simple case.</p>"},{"location":"developer_docs/references/design_choices/#architecture-qtsignals-between-ui-and-controller","title":"Architecture - QtSignals between UI and controller","text":"<p>Decision: UI widgets communicate with <code>MainWindow</code> exclusively via Qt Signals. No widget holds a reference to the controller. Why: - Decoupling - a widget can be tested in isolation without a controller or DCC. - Clarity - all connections are defined in one place (<code>MainWindow.create_gui</code>), making the data flow easy to trace. - Extensibility - swapping a widget for a new version only requires reconnecting its signals. <pre><code># All connections is one place - easy to audit\nself.rename_page. request_rename.connect(self.rename_nodes)\nself.prefix_suffix_page.request_prefix.connect(self.add_prefix)\n</code></pre></p>"},{"location":"developer_docs/references/design_choices/#architecture-json-presets-over-maya-optionvar","title":"Architecture - JSON presets over Maya <code>optionVar</code>","text":"<p>Decision: Presets are stored as JSON files in <code>resources/presets/</code>, not in Maya <code>optionVar</code> system. Why: - Portability -  JSON files can be committed to Git and shared across a team. <code>optionVar</code> is per-machine and per-Maya installation. - Readability - a JSON file is human-readable and editable outside Maya. - Version control - the default presets ship with the tool in <code>constants.py</code> and can be restored at any time.  </p> <p>Trade-off: The JSON files live inside the package - on a shared installation, all users share the same presets file. For multi-user studio deployment, the presets path should be moved to a user-specific location (e.g. <code>~/maya/scripts/crossrenamertool/presets/</code>)</p>"},{"location":"developer_docs/references/design_choices/#ux-two-buttons-for-fix-duplicates","title":"UX - Two buttons for Fix Duplicates","text":"<p>Decision: Fix Duplicates has two buttons - <code>Auto Rename Duplicates</code> and <code>Rename Selected</code> - plus an editable table. Why: A fully automatic fix is fast but dumb - it cannot know the intent behind the name. <code>arm</code> duplicated in <code>GRP_arm_L</code> and <code>GRP_arm_R</code> should become <code>arm_L</code> and <code>arm_R</code>, not <code>arm_001</code> and <code>arm_002</code>. The editable table lets the artist review and correct every proposed name before applying. <code>Auto Rename</code> fills the table with a sensible default; <code>Rename Selected</code> applies exactly what the artist has written.</p>"},{"location":"developer_docs/references/design_choices/#ux-tooltips-with-html","title":"UX - Tooltips with HTML","text":"<p>Decision: All widgets tooltips use HTML formatting (<code>&lt;b&gt;</code>, <code>&lt;i&gt;</code>, <code>&lt;code&gt;</code>, <code>&lt;p&gt;</code>). Why: Plain text tooltips are hard to scan. HTML allows separating the description from the example, bolding key terms, and using monospace for node names - making tooltips readable at a glance. <pre><code>self.step.setToolTip(\n    \"&lt;p&gt;Increment between each number.&lt;/p&gt;\"\n    \"&lt;p&gt;&lt;i&gt;Example: Start=&lt;b&gt;1&lt;/b&gt;, Step=&lt;b&gt;2&lt;/b&gt; -&gt; \"\n    \"&lt;span style='color:#5285a6;'&gt;arm_001, arm_003, arm_005&lt;/span&gt;&lt;/i&gt;&lt;/p&gt;\"\n)\n</code></pre></p>"},{"location":"user_manual/","title":"User Manual","text":"<p>Tutorials How-to Guides References Explanations</p>"},{"location":"user_manual/explanations/","title":"Explanations","text":"<p>Coming soon.</p>"},{"location":"user_manual/how-to-guides/","title":"How-to Guides","text":"<p>The How-to Guides section provides various guides on how to use a specific feature of the tool.</p> <p>Create Shelf button </p>"},{"location":"user_manual/how-to-guides/guides/create_shelf_button/","title":"Create Shelf button","text":"<p>This guide details the steps required to create a shelf button.</p> <ol> <li>In Maya, open the Script Editor (Windows&gt; General Editors &gt; Script Editor)  </li> <li>Copy the code below in a Python tab. <pre><code>import sys\n\npath = r\"path/to/module/src\"  # update this\nif path not in sys.path:\n    sys.path.append(path)\n\nfrom crossrenamertool.launcher import run\nrun()\n</code></pre></li> <li>Selection the code.  </li> <li>RMB &gt; Save Script to Shelf...  </li> <li>Name it : <code>Cross Renamer Tool</code>.  </li> <li>Cross Renamer Tool button is inside your shelf !  </li> </ol>"},{"location":"user_manual/references/","title":"References","text":"<p>Coming soon.</p>"},{"location":"user_manual/tutorials/","title":"Tutorials","text":"<p>Coming soon.</p>"}]}